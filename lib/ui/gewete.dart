import 'package:flutter/material.dart';

class GewetesList extends StatelessWidget {
  const GewetesList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
